[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = [
  "E",  # pycodestyle
  "F",  # pyflakes
  "I",  # import sort
  "B",  # bugbear
  "UP", # pyupgrade
]

ignore = []

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
